---
'@cherrystudio/ai-core': patch
---

Bump `@ai-sdk/deepseek` from 2.0.29 to 2.0.30 to pick up upstream fix preserving `reasoning_content` for `deepseek-v4` in multi-turn requests. Local patch adding the `reasoning_effort` option re-applies cleanly.
