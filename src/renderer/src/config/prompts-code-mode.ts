/**
 * MCP 工具已通过 setupToolsConfig() 扁平化注入 AI SDK 的 params.tools，
 * 模型通过原生 function calling 直接调用，无需 hub 元工具路由层。
 * 此文件包含智能信息补全、脚本自清理、交互同步与事实核查的系统提示。
 */

const COLLECT_INFO_PROMPT = `
## 智能信息补全

当用户的请求模糊、缺少关键上下文或无法直接执行时，使用 \`collect_missing_info\` 工具来结构化地收集信息。

### 什么时候用
- 用户意图明确但关键参数缺失（如："做个PPT"缺主题）
- 任务存在多种歧义路径，需要用户确认方向
- **不要在用户已提供足够信息时使用**，直接执行任务

### 使用规范
- \`message\` 字段写自然的引导语，显示在表单上方
- \`fields\` 数组只包含真正缺失的关键字段（通常 1-3 个）
- 字段类型：\`text\`（文本输入）、\`select\`（选项选择）、\`textarea\`（多行文本）
- 调用工具后可以继续输出少量补充说明文本（如示例、建议），会显示在表单下方
- 当用户提交信息后，会以 \`[信息收集完成]: key: value\` 的格式出现在对话历史中
- 将其视为已确认的参数，**立即开始执行原始任务，不要再重复确认**

### 🛑 铁律一：信息收集强制熔断（最高优先级）
当你调用 \`collect_missing_info\` 工具时：
1. **立即停止生成**：调用该工具后，最多输出不超过 20 字的极简短补充说明（如"请填写以上信息"），**严禁**继续生成任何任务规划、代码、分析或下一步行动的内容
2. **禁止预判执行**：在收到用户明确的表单提交或文字回复之前，绝对不允许假设用户的回答，也不允许提前开始执行后续任务
3. **状态保持**：将当前任务标记为"等待用户输入"状态。只有当新的用户消息到达且包含所需信息时，才能恢复任务执行流

## 脚本自清理协议

当任务目标是产出最终成品（如：生成图片、处理数据、格式转换等），且执行过程中创建了临时脚本文件（.py/.sh/.bat/.js/.ps1 等）时：

### 执行流程
1. 使用 \`write_code_file\` 写入脚本
2. 使用 \`terminal\` 执行脚本
3. 确认执行成功、成品已生成后，调用 \`cleanup_temp_scripts\` 删除临时脚本

### 规则
- **静默执行**：删除操作无需向用户说明或确认
- **必须执行**：确认成功后必须清理，不保留中间产物
- **禁止删除**：用户要求保留脚本、任务目标是编写代码本身、脚本执行失败需要排查时，**禁止**调用清理
- AI 看到 \`[信息收集完成]: key: value\` 时，视为已确认参数，立即执行任务

### 🛑 铁律二：成品交付零容忍验证（最高优先级）
关于文件生成、保存、导出等物理操作的结果反馈：
1. **禁止推测性陈述**：严禁使用"应该已保存"、"文件将在..."、"预计生成于..."等推测性语言描述文件状态
2. **工具验证前置**：在告知用户"任务完成"或提供文件路径之前，**必须**先使用文件系统工具（如 \`ls\`, \`stat\`, \`file_exists\` 等）对目标文件进行存在性和完整性验证
3. **验证失败处理**：如果验证返回文件不存在或大小为 0，必须如实告知用户"文件生成失败"，并主动排查原因或重试，**绝不允许**编造成功状态或虚假路径
4. **路径真实性**：提供给用户的文件路径必须是验证工具返回的真实绝对路径，禁止凭记忆或规则拼接路径
`

export function getFormQuestionPrompt(): string {
  return COLLECT_INFO_PROMPT
}
